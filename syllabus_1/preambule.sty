\usepackage[dvipsnames]{xcolor}
\usepackage[margin=1in]{geometry} % pour les marges
\usepackage[french]{babel} % le document est en français
\usepackage[T1]{fontenc} % encodage de la police
\usepackage[utf8]{inputenc} % encodage des caractères
\usepackage{mathtools} % macros pour les maths
%\usepackage{frenchmath} % conventions française pour les maths

%\usepackage{cmbright} % la police de caractère
\usepackage[sfmath,light]{kpfonts}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{calrsfs} % pour les beaux C de cercles avec la commande \mathcal{C}
\usepackage{tasks} % pour personnaliser les listes
\usepackage{fontawesome5} % plein de petites icones
\usepackage{graphicx} % insérer des images
\usepackage{tabularray} % pour les tableaux
\NewColumnType{C}[1][]{Q[c,co=1,#1]}
\usepackage{eurosym}
\usepackage[small]{cwpuzzle} % pour le mots croisé
\usepackage[group-minimum-digits=3, locale=FR]{siunitx} % unités
\usepackage[use-files, use-aux]{xsim} % pour la création d'exercices et des solutions
\xsimsetup{%
    path = xsim,
    load-style = syllabus-template,
    exercise/template = mytemplate,
    solution/template = mytemplate,
    exercise/within = chapter,
    exercise/the-counter= \arabic{exercise},
    blank/blank-style = \dotuline{#1},
}
\usepackage[normalem]{ulem} % pour les soulignement
\usepackage{calc} % pour faire des calculs sur les longueurs
\usepackage{xparse} % créer des macros évoluées
\usepackage{nicematrix}
\usepackage{tikzscale}
\usepackage{tikz} % pour le dessin
\usetikzlibrary{babel, mindmap, matrix, calc, positioning, math, shapes.geometric, tikzmark}
\usepackage{pgf-pie}
\usepackage{pgfplots}
\usepackage{tkz-base}
\usepackage{tkz-euclide} % surcouche de tikz
\usepackage[many]{tcolorbox} % pour les boîtes stylées
\usepackage{multicol} % pour créer des colonnes
\usepackage{enumitem} % pour personnaliser les listes
\setlist[enumerate]{label=\textbf{\alph*)}}
\SetEnumitemKey{cols}
{
    before = \begin{multicols}{#1},
    after = \end{multicols}
}
\usepackage{tcolorbox} % des boîtes stylées
\tcbuselibrary{skins,xparse}

% Exemple style
%===============================================================================
\newcommand{\exemple}{Exemple : }
\newcommand{\exemples}{Exemples : }
%===============================================================================

\usepackage{subfiles} % permet de générer un pdf par chapitre 
\usepackage[export]{adjustbox}

\usepackage{stmaryrd}

\newcommand{\corcolor}{white}
