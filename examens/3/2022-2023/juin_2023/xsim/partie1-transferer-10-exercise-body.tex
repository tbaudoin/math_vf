% ------------------------------------------------------------------------
% file `partie1-transferer-10-exercise-body.tex'
%   in folder `xsim/'
%
%     exercise of type `transferer' with id `10'
%
% generated by the `transferer' environment of the
%   `xsim' package v0.21 (2022/02/12)
% from source `partie1' on 2023/06/15 on line 406
% ------------------------------------------------------------------------
    \(M\) est un point mobile sur \([AD]\) à une distance \(x\) de \(D\). Déterminez l'ensemble des valeurs de \(x\) pour que le triangle \(MDC\) ait une aire plus petite que celle du triangle \(MAB\).

    \parbox{3cm}{%
        \(\lvert AD \rvert=10\)\medbreak
        \(\lvert HC \rvert=5\)\medbreak
        \(\lvert AB \rvert=4\)%
    }
    \hfil\input{tikz/point_mobile.tikz}
