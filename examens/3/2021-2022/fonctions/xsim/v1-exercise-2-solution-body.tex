% ------------------------------------------------------------------------
% file `v1-exercise-2-solution-body.tex'
%   in folder `xsim/'
%
%     solution of type `exercise' with id `2'
%
% generated by the `solution' environment of the
%   `xsim' package v0.21 (2022/02/12)
% from source `v1' on 2022/09/08 on line 57
% ------------------------------------------------------------------------
    1 point par justification correcte.
    \begin{enumerate}
        \item Faux.
        $\begin{aligned}[t]
            14 &\stackrel{?}{=} 3\cdot 4+ 4\\
            14 &\stackrel{?}{=} 12 + 4\\
            14 &\ne 16
        \end{aligned}$
        \item Faux. La racine d'une fonction est l'\textbf{abscisse} du point d'intersection de la fonction avec l'axe des abscisses. Ce n'est pas un point mais une valeur.
        \item Faux. $f(x)= 8 = x^0 + 8$, c'est une fonction du degré 0.
        \item Vrai. Lorsque $x$ augmente $f(x)$ passe des valeurs négatives aux valeurs positives, autrement dit $f(x)$ augmente également.
    \end{enumerate}
