\documentclass[12pt, parskip=half-]{scrartcl}
% \usepackage{showframe}
\usepackage[margin=1cm]{geometry}
\usepackage[french]{babel}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{mathtools}
\usepackage{tabularray}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage[charter]{mathdesign}
\usepackage{tikz}
\pagestyle{empty}

\begin{document}
\foreach \i in {1,...,3}{

Le but consiste à colorier des cases en noire dans chaque grille. Les chiffres donnés sur le côté et en haut de la grille vous donnent des indices. Ils indiquent la taille des cases noires qu'il faut colorier à la suite.

Par exemple 3,4 à gauche d'une ligne indique qu'il y a, de gauche à droite, un bloc de 3 cases noires puis un bloc de 4 cases noires sur cette ligne. Ces blocs sont espacés d'au moins une case blanche !

Si une ligne (ou une colonne) a sa première case noircie et que le premier chiffre de cette ligne est 3, vous pouvez noircir les deux cases suivantes, en effet le 3 correspond obligatoirement aux trois premières cases de la ligne. 
Cette astuce s'applique également pour la fin d'une ligne (ou d'une colonne).

Exemple : 

\includegraphics{img/exemple.png}

\dotfill

}
\newpage

\fbox{\includegraphics[height=8cm, width=8cm]{img/3.png}}
\fbox{\includegraphics[height=8cm, width=8cm]{img/1.png}}

\fbox{\includegraphics[height=8cm, width=8cm]{img/4.png}}
\fbox{\includegraphics[height=8cm, width=8cm]{img/1.png}}

\fbox{\includegraphics[height=8cm, width=8cm]{img/5.png}}

\end{document}